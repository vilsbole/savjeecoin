{
  "scripts": {
    "babel-node": "babel-node --presets=es2015",
    "clean": "rimraf dist",
    "build": "yarn clean && babel src -d dist",
    "start": "nodemon --exec yarn babel-node ./src/main.js"
  },
  "babel": {
    "presets": [
      "es2015"
    ]
  },
  "dependencies": {
    "crypto-js": "^3.1.9-1",
    "nodemon": "^1.18.3"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1",
    "rimraf": "^2.6.2"
  }
}
